{
    "version": 2,
    "builds": [
        {
            "api": "./index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "api": "/(.*)",
            "dest": "/"
        }
    ]
}